/**
 * A simple function to add two numbers together and return the sum.
 * @param  {number} x first number
 * @param  {number} y second number
 * @returns number sum
 */
export function add(x: number, y: number): number {
    return x + y;
}

console.log("\x1b[37m\x1b[4m\x1b[5mHello World! Coming from a Package\x1b[0m(ANSI is fun)...");
